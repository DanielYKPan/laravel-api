/*
describe('factory: WineService', function() {
    var WineService, httpBackend;

    beforeEach(inject('wines'));
    beforeEach(inject(function(_WineService_, $httpBackend) {
        WineService = _WineService_;
        httpBackend = $httpBackend;
    }));

   */
/* it('should fetch all required varieties', function(){

    });*//*


});
*/
